[project]
name = "blackkeys"
version = "0.0.1"
description = "Default template for PDM package"
authors = [
    {name = "gcca",email = "git@gcca.dev"},
]
dependencies = ["sanic>=25.12.1", "pylibmc>=1.6.3", "argon2-cffi>=25.1.0", "flatbuffers>=25.12.19", "aio-pika>=10.0.1", "httpx2>=2.12.0"]
requires-python = "==3.13.*"
readme = "README.md"
license = {text = "AGPL-GPL-3.0-or-later"}


[tool.pdm]
distribution = false
